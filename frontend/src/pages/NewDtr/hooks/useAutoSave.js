import { useEffect } from 'react'
import { useSelector } from 'react-redux'

export const useAutoSave = () => {
  // Auto-save functionality disabled to prevent form progress persistence
  // when users navigate between search and registration views
  
  // This hook is kept for potential future use but currently does nothing
  // to ensure clean form state when starting new candidate registrations
}